
       
        window.sr = ScrollReveal();
       

        // as a DOM node...
        var fooContainer = documento.GetElementById('fooContainer');
        sr.revelar('.foo',{contenedor: fooContainer});